"""
The schema’s entry-point for mutations. This acts as the public, top-level API from which all mutation queries must start.
"""
type Mutation {
    partyCreate(performerId:String ,
        partyGroupId: String,
        fullName: String,
        gentle: Gentle,
        phone: String,
        email: String,
        address: String,
        dob: DateCustom,
        description:String): Party

    partyGroupCreate(performerId:String ,
        name:String ,
        type:String ,
        shortName:String ,
        description:String
    ):PartyGroup

    partyLoginCreate(
        performerId:String ,
        userName:String ,
        password:String ,
        preferenceId:String ,
        preferenceType:String
    ):PartyLogin
}